어짜피 스크립트 원본을 읽을 수 있고, 이름을 바꿀 수 없는 기본함수나 API를 감지해서 점수를 매겼었기 때문에,
정말 간단한게 '민감한 API함수들'의 이름을 최대한 감추는데에 목적을 뒀습니다.

구조적인 교란을 주는 난독화는 구현X
데드코드나 더미함수도 구현X
미니파이 구현X

원본과 난독화 2개의 폴더를 올렸고, 
각 폴더에 제 로컬에서는 evidence가 뭐가 떴는지, 시간 순서로 전부 넣었습니다.

확장프로그램이 켜진 상태에서 npm start로 서버를 켜고 localhost3000에 들어가서 위젯을 닫고 키로깅을 위해 알파벳 몇개 눌렀습니다. 동작은 둘 다 동일하게 했습니다.
CMD에서 키로그는 정상적으로 다 떴습니다.